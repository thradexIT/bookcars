# MitoS R3 v2 — Real Browser Execution Marker

This file intentionally triggers the gated Mercado Pago TEST browser run for R3 v2.

Scope:
- isolated branch only;
- Mercado Pago TEST credentials only;
- no deployment;
- no merge;
- no production mutation;
- no changes to `main` or `developer`.

The resulting workflow run and sanitized artifact are the authority for the R3 v2 verdict. This marker by itself is not certification evidence.
